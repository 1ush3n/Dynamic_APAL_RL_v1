# 多数据集混合训练底座 (Multi-Dataset Foundation) 实施计划

为了将我们刚刚用生成器产出的各种不同大小、不同长短的随机数据集投入正式训练，且避免底层图网络和经验回放池（Replay Buffer/SIL）在重构张量时发生维度崩溃，我们需要对训练底座进行一次彻底的升维重构。

## 1. 配置梳理 (`configs.py`)

正如你所说，原有的 `dur_random_range` 和 `curriculum_episodes` 已经完成了它们的历史使命。我们将：
1. **删除冗余**：彻底移除 `dur_random_range` 和 `curriculum_episodes` 及其在 `environment.py` 中的残留逻辑。
2. **新增多数据集配置**：
   * `train_data_dir: str = 'data/random_datasets'`：训练集目录（存放批量生成的 CSV）。
   * `eval_data_path: str = 'data/290.csv'`：验证集路径（固定的基准图，用于衡量真实泛化水平）。
   * `enable_mixed_training: bool = True`：是否开启混合训练。

## 2. 仿真环境的多轨并行加载 (`environment.py`)

目前 `AirLineEnv_Graph` 在 `__init__` 时写死了只认一张图。当把不同大小的图混合塞进同一个 Batch 训练时，旧快照如果借用了新图的骨架进行重构，必定报出 `IndexError` 维度错位。

**改造方案 (池化骨架架构)**：
1. **输入升级**：`__init__(data_path_or_dir)` 将支持传入目录。
2. **骨架池化 (Dataset Pool)**：如果传入的是目录，自动扫描内部所有 `.csv/.xlsx`。将每一个数据集预解析并打包成一个独立的环境上下文对象（Context），包含：`raw_data`, `num_tasks`, `base_data`, `base_task_x`, `mean_task_time`, 等等。然后全部存入 `self.dataset_pool` 列表中。
3. **Episode 无缝切换**：在 `reset()` 中，只需一行代码 `idx = random.randint()`，瞬间切换当前回合的激活上下文。零 IO 延迟！
4. **快照溯源机制**：在 `get_state_snapshot()` 导出的字典里，打上一个烙印 `'dataset_idx': self.active_dataset_idx`。
5. **绝对安全的重构**：`rebuild_state_from_snapshot()` 读取这个烙印，去池子里拿**严格匹配它当时身形尺寸**的 `base_data` 进行重构。即使 Batch 里混杂了 5 种不同尺寸图的残局，也能完美拼接！

## 3. 训练主循环适配 (`train.py`)

1. **环境实例化**：
   * `env = AirLineEnv_Graph(configs.train_data_dir)` （传入整个训练图集）
   * `eval_env = AirLineEnv_Graph(configs.eval_data_path)` （传入单个验证图）
2. **消除外部尺寸依赖**：确保 `train.py` 中所有的张量遮罩（如 `task_mask`）都通过 `env.num_tasks` 动态获取当前回合的实际尺寸，而不是硬编码配置里的最大尺寸。

## 4. 开放问题 (Open Questions)

> [!WARNING]
> 这是一个底层架构的大手术，需要你的最终确认：
> 1. 上述的**骨架池化方案 (Dataset Pool)** 能够在不修改 PPOAgent 核心网络和 SIL 回放池代码的前提下，完美解决混合尺寸图的拼接问题。你觉得这个设计优雅吗？
> 2. `configs.py` 中的 `n_j`（最大任务数）现在已经变成了一个单纯的提示变量（网络不再需要它定长）。我是否可以保留它仅作参考，或者你需要我把它彻底删掉？

如果你对这个“多轨混合训练底座”的架构方案满意，请回复批准，我将开始大刀阔斧地重构。
